import React from 'react'

const SideBar = () => {
  return (

    // note : after comple push to sidebar branch

    <div>
        <div>
            sidebar
        </div>
    </div>
  )
}

export default SideBar